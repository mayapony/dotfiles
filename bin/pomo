#!/bin/sh

arg1=$1
shift
args="$*"

min=${arg1:?Example: pomo 15 Take a break}
sec=$((min * 60))
relaxsec=$((5 * 60))
msg="${args:?Example: pomo 15 Take a break}"

export XDG_SESSION_TYPE=wayland
swaylock

while true; do
	date '+%H:%M' && sleep "${sec:?}" && notify-send "${msg:?}"
done
