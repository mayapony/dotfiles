import picgo from 'picgo';
import { PluginConfig } from 'picgo/dist/utils/interfaces';
declare const _default: (ctx: picgo) => {
    register: () => void;
    guiMenu: (ctx: any) => {
        label: string;
        handle(ctx: picgo, { showNotification }: {
            showNotification: any;
        }): Promise<void>;
    }[];
    uploader: string;
    config: (ctx: picgo) => PluginConfig[];
};
export = _default;
